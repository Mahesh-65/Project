apiVersion: apps/v1
kind: Deployment
metadata:
  name: shop-service
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: shop-service
  template:
    metadata:
      labels:
        app: shop-service
    spec:
      containers:
      - name: shop-service
        image: maheshnandi/gamesbond-shop-service:v1.0.0
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 4006
        envFrom:
        - configMapRef:
            name: shop-service-devconfig
        - secretRef:
            name: shop-service-secret
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "300m"
            memory: "256Mi"
        securityContext:
          runAsNonRoot: true
          runAsUser: 1001
          allowPrivilegeEscalation: false
        readinessProbe:
          httpGet:
            path: /health
            port: 4006
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 4006
          initialDelaySeconds: 15
          periodSeconds: 20
