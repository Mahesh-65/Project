apiVersion: apps/v1
kind: Deployment
metadata:
  name: ground-service
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: ground-service
  template:
    metadata:
      labels:
        app: ground-service
    spec:
      containers:
      - name: ground-service
        image: gamesbond-ground-service:v1.0.0
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 4005
        envFrom:
        - configMapRef:
            name: ground-service-config
        - secretRef:
            name: ground-service-secret
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "300m"
            memory: "256Mi"
        securityContext:
          runAsNonRoot: true
          runAsUser: 1001
          allowPrivilegeEscalation: false
        readinessProbe:
          httpGet:
            path: /
            port: 4005
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /
            port: 4005
          initialDelaySeconds: 15
          periodSeconds: 20
