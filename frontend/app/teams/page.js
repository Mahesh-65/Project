export default function TeamsPage() {
  return (<section><h2>Teams</h2><div className="card"><p className="muted">Create teams, join via invite codes, and manage captains/members using team-service APIs.</p></div></section>);
}
