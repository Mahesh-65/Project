# Port number the frontend will run on
PORT=3000

# The URL for the backend API proxy
# For local dev without Docker, this would usually be your API gateway or specific service port
NEXT_PUBLIC_API_URL=http://localhost:4000

# Node environment (development or production)
NODE_ENV=development
